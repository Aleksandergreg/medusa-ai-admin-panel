/**
 * Medusa-specific terminology mappings and glossary
 */
export const MEDUSA_GLOSSARY = `MEDUSA GLOSSARY AND MAPPINGS:
- "discounts" → promotions 
- "items" → products 
- "shipping labels/rates" → shipping methods 
- "customers" → customers 
- "returns" → returns; "exchanges" → returns/exchanges`;
